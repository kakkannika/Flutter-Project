import 'package:flutter/material.dart';

class Account extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Welcome accout Screen',
        style:  TextStyle(fontSize: 24, fontWeight: FontWeight.bold),
      ),
    );
  }
}
